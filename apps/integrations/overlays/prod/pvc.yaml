apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wapi-assets-pvc
  labels:
    app.kubernetes.io/part-of: integrations
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
  storageClassName: nfs-shared
