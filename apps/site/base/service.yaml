apiVersion: v1
kind: Service
metadata:
  name: site
  labels:
    app.kubernetes.io/name: site
    app.kubernetes.io/part-of: site
spec:
  selector:
    app.kubernetes.io/name: site
  ports:
    - name: http
      port: 80
      targetPort: 80
